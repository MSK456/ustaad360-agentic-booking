# Development & Compilation Reference Commands

```bash
# Clean install
npm install

# TypeScript validation
npx tsc --noEmit

# Expo checklist
npx expo-doctor

# Launch local developer Metro server
npx expo start --clear --port 8083

# Build standalone Android APK
eas build -p android --profile preview --clear-cache
```
